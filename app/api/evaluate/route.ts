// TODO(api/evaluate): GET — evaluation metrics: deterministic vs AI-judged accuracy
// measured against ground truth, plus learning progress over time. No logic yet.
export async function GET() {
  return Response.json({ error: "Not implemented yet" }, { status: 501 });
}